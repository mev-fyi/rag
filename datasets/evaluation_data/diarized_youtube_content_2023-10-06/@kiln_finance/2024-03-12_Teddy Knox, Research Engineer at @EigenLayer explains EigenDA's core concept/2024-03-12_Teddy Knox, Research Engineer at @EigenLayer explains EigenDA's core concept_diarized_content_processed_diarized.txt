00:00:00.090 - 00:00:58.970, Speaker A: Eigenda is one of the first Abs on Eigen layer. This means that eigenda operators are secured by the restake from Eigen layer. And Eigenda's sort of core concept is that of allowing the user to own their relationship to DA. So what does this mean? This means that Eigenda not only scales, it scales to we've done tests that scale it all the way up to ten megabytes per second. But also Eigenda will let you stake potentially with a dual quorum model, which means that you can use your own your roll ups token to define a quorum that's separate from the main default quorum based on the Ethereum restaked. And so you can define a second quorum which is sort of secured by your own token.
